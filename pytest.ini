[pytest]
# Restrict test collection to tests/ only - never collect from production code
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*
# Ensure tests only run when explicitly invoked via pytest
addopts = -v --strict-markers
